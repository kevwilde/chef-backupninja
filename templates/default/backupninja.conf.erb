#######################################################
#      This configuration file is managed by Chef.    #
#            Do not edit this file manually.          #
#######################################################

# how verbose to make the logs
# 5 -- Debugging messages     (and below)
# 4 -- Informational messages (and below)
# 3 -- Warnings               (and below)
# 2 -- Errors                 (and below)
# 1 -- Fatal errors           (only)
loglevel = <%= node['backupninja']['loglevel'] %>

# send a summary of the backup status to
# this email address:
reportemail = <%= node['backupninja']['reportemail'] %>

# if set to 'yes', a report email will be generated
# even if all modules reported success. (default = yes)
reportsuccess = <%= node['backupninja']['reportsuccess'] %>

# if set to 'yes', a report email will be generated
# even if there was no error. (default = yes)
reportwarning = <%= node['backupninja']['reportwarning'] %>

# where to log:
logfile = <%= node['backupninja']['logfile'] %>

# directory where all the backup configuration files live
configdirectory = <%= node['backupninja']['configdirectory'] %>

# where backupninja handler scripts are found
scriptdirectory = <%= node['backupninja']['scriptdirectory'] %>

# use colors in the log file
usecolors = <%= node['backupninja']['usecolors'] %>
